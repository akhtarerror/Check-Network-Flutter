name: network_info_app
description: A Flutter app to display network information including IP, SSID, BSSID, and connection status.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dart_ipify: ^1.1.1
  network_info_plus: ^4.0.0
  connectivity_plus: ^3.0.6
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
